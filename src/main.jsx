import React from 'react'
import ReactDOM from 'react-dom/client'
import JiraWriter from './jira-writer/jira-writer'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <JiraWriter/>
  </React.StrictMode>,
)
